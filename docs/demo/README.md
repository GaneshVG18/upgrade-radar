# Demo assets

`report.html` and `report.png` are generated from `node dist/cli.js demo --out artifacts/demo`. The report carries a permanent `ILLUSTRATIVE FIXTURE` badge. It uses authored fixture decisions and contains no live TypeSafe response, service performance number, API key, or private evaluation material.
